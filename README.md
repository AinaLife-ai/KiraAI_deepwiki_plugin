<h1 align="center">🔍 DeepWiki 助手</h1>

<p align="center">
  <strong>KiraAI 插件 · 通过 DeepWiki MCP 查询 GitHub 仓库文档</strong>
</p>

<p align="center">
  支持多路搜索、<code>/dw</code> 命令（含 <code>@机器人</code> 唤醒）、
<p align="center">
  候选序号选择、查询失败自动重置、合并转发与 JSON 可配置引导文案。
</p>

---

## 📦 快速开始

1. 将本目录放到 `data/plugins/deepwiki_plugin/`
2. 在 WebUI 中启用插件
3. 打开 **自定义命令 → 启用命令控制**
4. 发送命令：

```bash
/dw KiraAI
```

按提示用 `/dw 1` 选择仓库，或 `/dw owner/repo 你的问题` 直接查询。

---

## ⌨️ 命令用法

> 默认前缀 `/dw`，可在配置中自定义命令词。

| 命令 | 作用 |
|------|------|
| `/dw <关键词>` | 搜索候选仓库（**不会**自动绑定第一个） |
| `/dw 1` / `/dw 2` | 按序号选择候选并查询 |
| `/dw owner/repo` | 直接指定仓库（使用默认问题） |
| `/dw owner/repo <问题>` | 指定仓库 + 自定义问题 |
| `/dw <问题>` | 在**已有上下文**上继续追问 |
| `/dw ?` | 查看当前上下文仓库 |
| `/dw clear` | 清除上下文（预设仓库默认保留绑定，见下文） |

> **注意**：状态/清除词必须写在命令后面，即 **`/dw ?`**、**`/dw clear`**，而非单独的 `?` / `clear`。

### @ 唤醒

群聊中同样支持：

```bash
@安魂曲 /dw KiraAI
@安魂曲 /dw 1
```

会走命令路径，**不进入 LLM**。

---

## ⚙️ 配置分组

| 分组 | 内容 |
|------|------|
| **MCP 连接与认证** | 开关、端点、超时、GitHub Token |
| **缓存与知识库** | TTL、可选存 KiraKB |
| **仓库搜索优化** | 候选数量、多路搜索、候选额外提示 |
| **自定义命令-基础** | 启用 `/dw`、命令词、默认问题 |
| **自定义命令-上下文管理** | 用户隔离、预设仓库、重置保留开关、clear/status 词 |
| **LLM 工具** | 是否允许自然语言工具调用 |
| **合并转发-基础** | 总开关、强制转发、长度阈值、单节点字数、API 超时、QQ 文本样式 |
| **合并转发-卡片内容** | 引导语、卡片元数据、操作指南 |
| **合并转发-失败降级与兼容** | 私聊接口回退、转发失败降级普通消息 |

---

## 🧠 LLM 工具（可选）

| 工具 | 说明 |
|------|------|
| `search_deepwiki` | 搜索 GitHub 仓库，返回候选列表 |
| `ask_deepwiki` | 向指定仓库提问 |
| `get_deepwiki_structure` | 获取仓库文档结构/目录 |
| `read_deepwiki_content` | 读取指定主题的文档内容 |

关闭「允许自然语言调用」后仅命令模式可用。

---

## 📋 行为细节

### 1. 候选列表与操作说明

搜索后会列出候选，并附带选仓指引 —— `/dw 1` 选第一个、`/dw owner/repo` 直接指定、`/dw ?` / `/dw clear` 查看或清除上下文。

### 2. 下划线与代码保护

`sanitize` / `stylize` 会保护以下内容不被误处理：

- 代码块 `` ``` ... ``` ``
- 行内代码 `` `...` ``
- URL 链接
- **snake_case / 路径中的 `_` 一律保留**

`stylize` 样式：

- `## 标题` / `**粗体**` → `【标题】` / `【ＡＰＩ Ｋｅｙ】`

### 3. 引导语 / 操作指南使用 JSON

配置中用 `lines` 数组管理换行：

```json
{
  "lines": [
    "【DeepWiki 查询】",
    "仓库：{repo}",
    "问题：{question}",
    ""
  ]
}
```

支持的模板字段：`intro_template`、`forward_metadata_template`、`operation_guide_text`、`candidate_hint_text`。

### 4. 查询失败自动重置

若 DeepWiki 返回类似错误：

```
Repository not found. Visit https://deepwiki.com to index it.
```

插件会：① 提示失败原因 → ② **自动清除当前上下文** → ③ 提示重新 `/dw <关键词>` 或换 `owner/repo`。避免「选中坏仓库后，再发关键词却变成追问」的问题。

### 5. 切换项目

- 推荐：`/dw clear` → `/dw 新关键词`
- 无上下文时：直接 `/dw 新关键词` 会搜索
- 有上下文时：若输入像「单独关键词」也会重新搜索（启发式）；完整问句仍当追问

### 6. 预设仓库与重置

配置「默认初始仓库列表」（格式 `ctx;owner/repo`）后，对应会话/用户首次使用 `/dw` 会自动绑定预设仓库。`ctx` 支持完整 sid（`qq:gm:群号`）、忽略适配器名（`gm:群号`）、裸 ID（`群号`）、用户（`user:QQ`）——适配器名写错也能按「类型:ID」匹配上。

预设会话的行为：

- `/dw`（空参）：显示当前绑定的仓库与操作指南，而非通用用法
- `/dw <问题>`：直接向预设仓库提问（**单 token 如「安装」也按问题处理**，不会被误判成搜索）
- 想改查其他仓库：`/dw owner/repo` 显式指定

开启「上下文按用户隔离」时：`user:QQ` 专属预设优先；没有专属预设的用户，自动被所在会话的会话级预设接管。

默认开启「重置时保留预设仓库」：`/dw clear` 只清除对话记忆与候选列表，**仍保持绑定预设仓库**，可直接继续提问，不会回到搜索阶段。关闭该开关后，重置即彻底清空（下一次使用命令时预设会重新应用）。

---

## 👤 作者

**znq19**

---

<details>
<summary><strong>📝 更新日志</strong></summary>

### 2.4.0

- 新增「LLM 调用默认绑定预设仓库」开关（默认关）：开启后，用户自然语言提问（或 Bot 主动调用 DeepWiki 工具）时默认使用当前会话/用户绑定的仓库，无需先 `search_deepwiki`——通过 `@on.llm_request` 在系统提示注入默认仓库；`ask_deepwiki` 的 repo 参数非法时用绑定仓库兜底，成功调用后记为当前绑定；`/dw clear` 后同样回落到预设仓库

### 2.3.2

- **用户隔离模式下会话级预设接管**：开启「上下文按用户隔离」时 ctx 为 `user:QQ`，此前会话级预设（`qq:gm:群号`）匹配不上导致预设失效。现在分级解析：`user:` 专属预设优先，未命中的用户自动回落到所在会话的会话级预设

### 2.3.1

- **修复升级后预设/旧配置丢失**：KiraAI 的 `_ensure_plugin_config` 会按新 schema 给新分组补默认值且保留旧分组，WebUI 增量保存也不清旧分组——上一版简单「新覆盖旧」导致新分组的默认值顶掉旧分组里的用户配置。改为逐键智能合并：新分组值等于 schema 默认值时回退使用旧分组用户值，并打 warning 日志提示
- 新增诊断日志：初始化时输出 `enable_command / presets / reset_keeps_preset_repo / isolate_by_user / force_forward_all` 解析结果；每次 `/dw` 命令输出 `ctx / query / bound`，便于核对上下文 key 与预设匹配
- `_get_sid` 增加兜底：某些 core 版本事件无 `session` 属性时，从 adapter/group/sender 自行组装 sid，避免上下文 key 全部落到 `"default"` 导致预设无法匹配

### 2.3.0

- **修复合并转发失败的真正根因**：转发节点 `uin` 取自不存在的 `event.self_id`，恒为 `"0"`，导致 NapCat 伪造转发被拒；已改为正确读取 `event.message.self_id`
- 转发失败重试策略优化：惰性构建节点、后续尝试递减超时、首个 action 超时后不再无意义换名重试
- 非 OneBot 适配器（无 `send_action`）自动降级为普通消息，不再反复尝试转发
- 新增「重置时保留预设仓库」开关（默认开）：预设仓库的会话/用户用 `/dw clear` 后仍保持绑定
- **预设仓库匹配忽略适配器名**（按「类型:ID」匹配，也支持 `gm:群号`/裸 ID 写法），适配器名写错预设也能生效
- 预设会话 `/dw` 空参显示当前绑定仓库与操作指南；预设锁定期间单 token 输入按问题处理
- 配置分组细分：自定义命令拆为「基础/上下文管理」，合并转发拆为「基础/卡片内容/失败降级与兼容」

### 2.1.0

- 修复 `name 'Any' is not defined` 加载失败
- 修复 section 配置读取、缓存/上下文未初始化
- 候选列表操作说明；搜索不再自动绑定第一项
- 查询失败（未索引等）自动重置上下文
- 支持 `@机器人 /dw` 命令拦截
- 操作指南明确 `/dw ?`、`/dw clear` 前缀
- 文本处理保留下划线与代码；stylize 改为中英一致强调
- 引导/指南 JSON 配置完善

### 2.0.0

- 多路搜索、命令触发、合并转发等

</details>
